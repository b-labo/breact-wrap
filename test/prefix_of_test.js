/**
 * Test case for prefixOf.
 * Runs with mocha.
 */
'use strict'

const prefixOf = require('../lib/prefix_of.js')
const assert = require('assert')
const co = require('co')

describe('prefix-of', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Prefix of', () => co(function * () {

  }))
})

/* global describe, before, after, it */
